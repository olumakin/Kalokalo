numpy>=1.26
scipy>=1.11
pandas>=2.1
pydantic>=2.4
pyarrow>=14.0
pytest>=7.4
# >=1.42 for st.login/st.user/st.logout (OIDC admin auth, Phase 0 revised) —
# the actual floor, not just what happened to already be installed. The
# [auth] extra pulls in Authlib>=1.3.2, which st.login() requires at
# runtime and does not otherwise install.
streamlit[auth]>=1.42
requests>=2.31
PyYAML>=6.0
# supabase>=2.10 is required for the new sb_publishable_/sb_secret_ key
# format (Supabase's newer key scheme) — 2.3.0 rejects it locally via a
# hardcoded JWT-shaped regex before ever making a network call. Pinned
# to what was actually verified against real credentials in this repo.
supabase==2.31.0
